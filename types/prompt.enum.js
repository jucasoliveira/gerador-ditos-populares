export const prompt = (name, adverbios) => {
  return `Crie um ditado de uma linha começando com ${name} , adicionando um adjetivo para ${name}, a palavra não, adicionando um verbo, e adicionando o advérbio ${adverbios}.`;
};
