export const prompt = (name, adverbios) => {
  return `Crie um ditado com uma linha começando com ${name} ; adicionando um adjetivo para ${name}; a palavra não; um verbo; um advérbio de lugar ou hora`;
};

export const promptEnglish = (name, adverbios) => {
  return `Create a phrase starting with ${name} ; adding an adjective to ${name}; the word not; a verb; an adverb of place or time`;
};
